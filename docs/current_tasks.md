# 🍆 Пипирик Варс — Текущие задачи

> Этот файл описывает **только то, что в работе сейчас**: активная feature-ветка, активный спринт/PR, чек-лист текущих шагов и их статусы. По мере выполнения шаги отмечаются `[x]`.
>
> **С 2026-05-08:** обновления `history.md` и `current_tasks.md` под следующий PR делаются **внутри самого фичевого PR** (последним коммитом перед мерджем). Отдельный postmerge-PR больше не открывается — см. [`../CONTRIBUTING.md`](../CONTRIBUTING.md) «Перед мерджем PR-а».
>
> **Длинный план** (фазы / спринты A→Z) — в [`development_plan.md`](development_plan.md).
> **Игровая спецификация** (механики, формулы, баланс) — в [`game_design.md`](game_design.md).
> **Журнал завершённых работ** — в [`history.md`](history.md).
> **Правила работы с документацией + протокол передачи задач между агентами** — в [`../CONTRIBUTING.md`](../CONTRIBUTING.md).
>
> ⚠️ **Перед каждым коммитом** обнови чек-лист ниже (отметь готовые шаги, обнови «текущая позиция»). Это нужно для непрерывности при смене агентов — следующий агент должен знать, где ты остановился.

---

## 📸 Снимок состояния проекта

> Эта секция отражает состояние проекта **на момент последнего обновления этого файла**. Она нужна для того, чтобы новый агент за 30 секунд понял, что происходит. Обновляй её при старте/завершении каждого PR-а.

**На `main` после мерджа PR 4.1-B:** последний смерженный PR — **4.1-B** — Призовой пул + persistence + audit. Domain-агрегат `PrizePool(stars, ton_nano, usdt_decimal)` с invariant-ом `>= 0` + иммутабельным `apply_increment(currency, amount_native)` + VO `StarsPoolBalance`/`TonNanoAmount`/`UsdtDecimalAmount` + порт `IPrizePoolRepository`; use-case `RecordDonation(currency, payment_amount_native, idempotency_key) -> RecordDonationResult` (10% от подтверждённого платежа → пул через `floor-division (// 10)`, audit-запись `PRIZE_POOL_INCREMENT` со scope `:prize_pool` отдельно от `:payment`); persistence — таблица `prize_pool_balance` + Alembic `0027_prize_pool_balance` (initial-seed 3 row-а per currency) + ORM `PrizePoolBalanceORM` + `SqlAlchemyPrizePoolRepository` (`get_current` / `apply_increment` через row-lock UPDATE); Alembic `0028_audit_source_prize_pool_increment` расширяет `audit_log_source_whitelist`; интеграция в `SpinPaidRoulette`-flow (Step 5b: `record_donation.execute(...)` после Step 5 `audit PAYMENT_RECORDED`); composition root в `bot/main.py`. **Без** TON Connect / USDT-провайдера / лот-генератора / `ClaimPrize` — это следующие PR-ы (4.1-C…F). Перед `4.1-B`: **CI cost-cut** (PR #129, `da7100a`) — pytest-xdist + py3.12-only + paths-ignore docs (≈52% экономии CI-минут); **4.1-A** (PR #128, `21c21c0`) — Telegram Stars skeleton; **3.6-B** (PR #127, `b684679`); **3.6-A** (PR #126); **3.5-D** (PR #125); **3.5-A→C** (#121–#123); **fix(load-tests)** (#124); **3.4-A→D** (#117–#120); **3.6 design doc** (#116); **3.3-A→D** (#112–#115); **3.2-A→D** (#108–#111); **3.1-A→E** (#99–#107). **Закрыты Спринты 3.1–3.6** (Фаза 3) + **Спринт 4.1, PR-ы A и B** (Фаза 4). **В работе Фаза 4 «Монетизация и масштаб»** ([`development_plan.md`](development_plan.md) §7) — **Спринт 4.1**, активный PR **4.1-C «Лот-генератор + крипто-приз в рулетке»**.

**Текущая ветка** — `devin/1778438123-sprint-4-1-C-lot-generator` (создана от `main = 93148aa`, merge PR #130) — **Спринт 4.1-C «Лот-генератор + крипто-приз в рулетке»** (третий PR Спринта 4.1). Активный шаг: **C.6** — резервирование лота в use-case-ах спинов + audit `PRIZE_LOT_RESERVED`. Готовы: C.0 (snapshot pivot + sticky HANDOFF), C.1 (Domain `PrizeLot` aggregate + VO `FeeBufferAmount` + ports `IPrizeLotRepository` / `IFeeEstimator` + errors + 67 unit-тестов), C.2 (Application use-case `GeneratePrizeLots` + audit-source `PRIZE_LOT_GENERATED` + Alembic `0029` + 41 unit-тест), C.3 (Alembic `0030_prize_lots` + `PrizeLotORM` + `SqlAlchemyPrizeLotRepository` + 33 integration-теста), C.4 (audit-source `PRIZE_LOT_REFUNDED` + Alembic `0031`), C.5 (picker `active_lots: Sequence[PrizeLot]` + `RouletteOutcome.crypto_lot(lot_id)`-фабрика + invariants `lot_id >= 1` IFF `CRYPTO_LOT` + 25 новых unit-тестов).

**Roadmap (Спринт 4.1, декомпозиция на фичевые PR-ы):**
- **4.1-A — Telegram Stars + платная рулетка (skeleton)** (задачи 4.1.1, 4.1.4) — **закрыт PR #128**: domain-payments + paid roulette + use-case `SpinPaidRoulette` + порт `IPaymentLedger` + persistence + audit + конфиг + bot-handler skeleton + 26 локалей × RU+EN.
- **4.1-B — Призовой пул + persistence + audit** (задачи 4.1.5, 4.1.6) — **закрыт PR 4.1-B**: domain-агрегат `PrizePool(stars, ton_nano, usdt_decimal)` + VO + `IPrizePoolRepository` + use-case `RecordDonation` (10% → пул, идемпотентно) + persistence (`prize_pool_balance` + Alembic `0027` + ORM + repository) + audit-source `PRIZE_POOL_INCREMENT` (Alembic `0028`) + интеграция в `SpinPaidRoulette`-flow (Step 5b) + composition root.
- **4.1-C — Лот-генератор + крипто-приз в рулетке** (задачи 4.1.7, 4.1.8) — **активный**: `PrizePoolService.regenerate_lots`, `IFeeEstimator` (P95 за 7 дней), cron 1×/час + триггер после крупного донат-зачисления; minLot = 1 USD-эквивалент + комиссия, maxLot = 10 USD-эквивалент; picker крипто-приза в результат-пуле free + paid рулеток (если активных лотов в данной валюте нет — слот занимает СМ-приз).
- **4.1-D — TON Connect + USDT + ClaimPrize** (задачи 4.1.2, 4.1.3, 4.1.9): TON SDK, USDT через TON-процессор, `ClaimPrize`, handler «Привязать кошелёк», integration в sandbox; signature-верификация TG Stars payload-а (вывод 4.1-A handler-а в продакшн).
- **4.1-E — Админ-команды + лимиты выплат** (задачи 4.1.10, 4.1.11): `/prize_pool`/`/refund_lot`/`/freeze_payouts` (super_admin + TOTP), rolling-30d-лимит.
- **4.1-F — Redis + ИИ + многоязычность + метрики + закрытие Спринта 4.1** (задачи 4.1.12, 4.1.13, 4.1.14, 4.1.15): переход на Redis, опц. ИИ-предсказания, +6 локалей (PT, ES, TR, ID, FA, UK), Prometheus + Grafana, финальный док-коммит.

---

## 🎯 Активный спринт — Спринт 4.1 «Монетизация и масштаб» 🎯 (Фаза 4)

> Цель спринта (по [`development_plan.md`](development_plan.md) §7 «Фаза 4 — Монетизация и масштаб», ГДД §12.5/§12.6): запуск платных каналов (Telegram Stars / TON / USDT) + крипто-призовой пул из 10% донат-зачислений + лот-генератор + крипто-приз в рулетке + use-case `ClaimPrize` + админ-команды + переход на Redis + многоязычность + метрики. Декомпозиция на 6 фичевых PR-ов (4.1-A → 4.1-F, см. Roadmap выше).

**Скоуп — задачи плана 4.1.* (детали — в [`development_plan.md`](development_plan.md) §7):**

- **4.1.1 — Telegram Stars: платная рулетка** ✅ — реализовано в 4.1-A (PR #128). Skeleton: invoice + pre_checkout_query + successful_payment → `SpinPaidRoulette`. До 4.1-D — staging-only.
- **4.1.2 — TON Connect:** фикс длина за TON. Sandbox + продакшн-сеть; webhook/poll платежей. (4.1-D)
- **4.1.3 — USDT (через TON-сеть/процессор):** параметризованные суммы → длина. (4.1-D)
- **4.1.4 — Антифрод платежей,** проверка двойных зачислений ✅ — реализовано в 4.1-A (PR #128) — `IdempotencyKey` VO + DB `UNIQUE (player_id, idempotency_key)` + on-conflict + anti-fraud-сверка currency/amount.
- **4.1.5 — 10% от каждого донат-зачисления → крипто-призовой пул** (ГДД §12.6) ✅ — реализовано в 4.1-B: `RecordDonation` use-case при подтверждении платежа делает второй проводкой `apply_increment(currency, donation = payment_amount_native // 10)`. Идемпотентно через scope `:prize_pool` поверх root-`idempotency_key`-а платежа.
- **4.1.6 — Призовой пул** ✅ — реализовано в 4.1-B: domain-агрегат `PrizePool(stars, ton_nano, usdt_decimal)`, persistence (`prize_pool_balance`), Alembic `0027`, audit `PRIZE_POOL_INCREMENT` (`0028`).
- **4.1.7 — Лот-генератор** (`PrizePoolService.regenerate_lots`, ГДД §12.6.3) — **в работе 4.1-C**. Cron 1×/час + триггер после крупного донат-зачисления. Учёт комиссии: `IFeeEstimator` с P95 за 7 дней. Минимум лота = 1 USD-эквивалент + комиссия; максимум = 10 USD-эквивалент. Юнит-тесты: пул 3 USDT → 3 лота × 1 USDT; пул 15 USDT → 1 лот × 10 USDT (5 USDT остаются); комиссия > buffer → лот возвращается.
- **4.1.8 — Крипто-приз** в результат-пуле платной + free-рулеток (ГДД §12.4.2, §12.5.2) — **в работе 4.1-C**. Если активных лотов в данной валюте нет — слот криптоприза занимает СМ-приз. Юнит-тесты picker-а: пул пуст → крипто-приз не появляется.
- **4.1.9 — Выплата выигрыша:** handler «Привязать кошелёк» + use-case `ClaimPrize(player, lot_id, recipient_address)` + транзакция через TON SDK. Жёсткая защита: `actual_fee > fee_buffer` → лот возвращается в пул, выплата откладывается. Юнит-тесты на все ветки; integration в TON sandbox. (4.1-D)
- **4.1.10 — Админ-команды** `/prize_pool`, `/refund_lot <lot_id>`, `/freeze_payouts` (super_admin + TOTP, ГДД §12.6.6). RBAC-тесты; audit-записи `ADMIN_PRIZE_*`. (4.1-E)
- **4.1.11 — Лимиты выплат на игрока:** `max 50 USDT-экв за 30 дней` (TODO(balance): финальное число). Сверх лимита — выплата ставится в очередь. Юнит-тесты на rolling 30 day window. (4.1-E)
- **4.1.12 — Переход на Redis** (лобби, очереди, DAU, locks). Нагрузочный тест 10× от MVP. (4.1-F)
- **4.1.13 — Перевод предсказаний/логов на ИИ** (опционально). Кэш на сгенерированных ответах. (4.1-F)
- **4.1.14 — Доп. языки:** PT, ES, TR, ID, FA, UK. Файлы переводов, тест fallback. (4.1-F)
- **4.1.15 — Метрики и дашборд** (Prometheus + Grafana). Графики DAU/RPS/караваны/рейды + крипто-пул per currency. (4.1-F)

**Финальный коммит каждого PR-а Спринта 4.1** (внутри ветки, последним перед мерджем) — обновить `history.md` (запись «Спринт 4.1-X: ...») + пересобрать «Снимок состояния» в `current_tasks.md` под `main = <коммит_слияния>`, передвинуть чек-лист на следующий PR (или закрыть Спринт 4.1 на 4.1-F и расписать чек-лист **первого PR-а Спринта 4.2**, если он спланирован).

---

## 📝 Чек-лист текущего PR — 4.1-C «Лот-генератор + крипто-приз в рулетке»

> Скоуп: application-сервис `PrizePoolService.regenerate_lots(currency)` (ГДД §12.6.3) — на cron 1×/час + триггер после крупного донат-зачисления нарезает свободный остаток `PrizePool` на лоты заданной величины (min `1 USD-эквивалент + комиссия` / max `10 USD-эквивалент`); domain-port `IFeeEstimator.estimate_fee(currency, target_amount_native) -> int` (P95 газа за 7 дней; имплементация в-памяти на старте, в 4.1-D перейдёт на TON RPC); domain-агрегат `PrizeLot(id, currency, amount_native, fee_buffer_native, status: ACTIVE|RESERVED|CLAIMED|REFUNDED, created_at, claimed_at)` + invariant-ы (`amount_native > fee_buffer_native >= 0`); persistence (таблица `prize_lots` + миграция Alembic `0029` + ORM + `SqlAlchemyPrizeLotRepository`); audit-source `prize_lot_generated` / `prize_lot_refunded`; picker крипто-приза в результат-пуле free + paid рулеток (`pick_outcome` берёт активный лот из `IPrizeLotRepository.list_active(currency)` если pool-кодрейн `crypto_lot` сработал; если активных лотов нет — слот занимает `LengthGain`-fallback как сейчас в 4.1-A); резервирование лота (`status=ACTIVE → RESERVED`) при выпадении в спин-результате — выплата отложена до `ClaimPrize` (4.1-D). Юнит-тесты лот-генератора: пул 3 USDT → 3 лота × 1 USDT; пул 15 USDT → 1 лот × 10 USDT (5 USDT остаются); `actual_fee > fee_buffer` → лот возвращается в пул со статусом `REFUNDED`; juнит-тесты picker-а: пул пуст → крипто-приз не выпадает (LengthGain-fallback).

- [x] `git fetch && git checkout main && git pull` (свежий `main = 93148aa` после мерджа PR #130).
- [x] Создать ветку `devin/1778438123-sprint-4-1-C-lot-generator` от свежего `main`.
- [x] **C.0 — Обновить `current_tasks.md`** под старт Спринта 4.1-C: пересобрать «Снимок состояния» под актуальный `main`, переписать чек-лист C.0–C.N под фактическую декомпозицию шагов, создать `AGENT_HANDOFF.md` (sticky-режим). Сделано в коммите `8b899fc`.
- [x] **C.1 — Domain `PrizeLot` aggregate + VO + порт + ошибки** (`src/pipirik_wars/domain/monetization/`): `PrizeLot` (frozen+slots) — `id, currency, amount_native, fee_buffer_native, status: PrizeLotStatus.{ACTIVE,RESERVED,CLAIMED,REFUNDED}, created_at, claimed_at` с invariant-ами (`amount_native > fee_buffer_native >= 0`, status-machine: `ACTIVE → RESERVED|REFUNDED`, `RESERVED → CLAIMED|REFUNDED`, `CLAIMED|REFUNDED` — terminal), immutable transitions `reserve()` / `claim(claimed_at=...)` / `refund()`. VO `FeeBufferAmount(int, >= 0)`. Порт `IPrizeLotRepository` (`list_active(currency)`, `add(lot)`, `update_status(lot_id, new_status, claimed_at?)`, `get_by_id(lot_id)`). Порт `IFeeEstimator.estimate_fee(currency, target_amount_native) -> int`. Ошибки: `PrizeLotInvariantError`, `PrizeLotStatusTransitionError`, `PrizeLotNotFoundError`. 67 unit-тестов на агрегат + VO + status-machine + errors + immutability.
- [x] **C.2 — Application use-case `GeneratePrizeLots`** (`src/pipirik_wars/application/monetization/generate_prize_lots.py`): принимает `(currency, idempotency_key) -> GeneratePrizeLotsResult`; читает `IPrizePoolRepository.get_current()`, для свободного остатка пула в данной валюте генерирует серию лотов (`min: 1 USD-эквив + estimate_fee`, `max: 10 USD-эквив`); каждый лот — `IPrizeLotRepository.add(...)` + `IPrizePoolRepository.apply_increment(currency, -lot.amount_native)` (декремент пула); audit-source `PRIZE_LOT_GENERATED`. Идемпотентно по `idempotency_key`-у (root-скоуп `prize_lot_generator:<currency>|<key>`, audit per lot с `<root>:lot:<idx>`-суффиксом). Sanity check: если `fee >= target_usd_native` — 0 лотов (защита от спайка газа). 41 unit-тест (max/min режим, below-min, все 3 валюты, идемпотентность, fee буфер, audit per lot, декремент пула, UoW commit). Сделано в этом коммите: + Alembic `0029_audit_source_prize_lot_generated` (расширение whitelist `audit_log.source`).
- [x] **C.3 — Persistence prize_lots** (`src/pipirik_wars/infrastructure/db/`): таблица `prize_lots` (`id BIGSERIAL`, `currency VARCHAR(16) NOT NULL CHECK (...)`, `amount_native NUMERIC(38,0) NOT NULL CHECK >= 1`, `fee_buffer_native NUMERIC(38,0) NOT NULL CHECK >= 0`, `status VARCHAR(16) NOT NULL CHECK (...)`, `created_at TIMESTAMPTZ NOT NULL`, `claimed_at TIMESTAMPTZ NULL`, + CHECK `amount_native > fee_buffer_native`, + CHECK `claimed_at IFF status='claimed'`); Alembic-миграция `0030_prize_lots` (без initial-seed-а — лоты on-demand) + индекс `ix_prize_lots_status_currency` + ORM `PrizeLotORM` (`with_variant(Integer, 'sqlite')` для portability) + `SqlAlchemyPrizeLotRepository` (`add` через `session.add+flush`, `update_status` через атомарный `UPDATE ... WHERE id=:id AND status IN (валидные source-статусы)`, fallback SELECT для различения `PrizeLotNotFoundError` vs `PrizeLotStatusTransitionError`, `_ensure_utc` нормализация TZ для aiosqlite-квирка). 33 integration-теста: round-trip add/get_by_id, все 3 валюты, status-machine `ACTIVE → RESERVED → CLAIMED|REFUNDED`, `list_active` фильтрация по currency и status с `ORDER BY id ASC`, double-reserve race → transition-error, terminal-status guard, все 7 DB-CHECK-ограничений last-line-of-defense. Обновлены `tests/integration/db/test_migrations.py` (`test_0030_descends_from_0029`, expected_revisions, versions_dir-listing, expected-tables) + `tests/integration/db/conftest.py` (импорт `PrizeLotORM`).
- [x] **C.4 — Audit-source `prize_lot_refunded`** (`domain/shared/ports/audit.py` + Alembic): добавлены `AuditAction.PRIZE_LOT_REFUNDED` + `AuditSource.PRIZE_LOT_REFUNDED` (оба = `"prize_lot_refunded"`); Alembic-миграция `0031_audit_source_prize_lot_refunded` расширила `audit_log_source_whitelist` (drop+create CHECK через `op.batch_alter_table` SQLite-совместимо). Размечено для будущего use-case-а `RefundPrizeLot`: `target_kind="prize_lot"`, `target_id="<lot_id>:refund"`, `after={lot_id, currency, amount_native, prev_status, pool_after_native, reason}`. Не входит ни в один из anticheat-source-вайтлистов. Обновлены `tests/unit/domain/shared/ports/test_audit_source.py` (`_load_migration_whitelist` ведёт на 0031) и `tests/integration/db/test_migrations.py` (`test_0031_descends_from_0030` + expected_revisions + versions_dir-listing). NB: `prize_lot_generated` добавлен в C.2 (Alembic `0029`).
- [x] **C.5 — Picker крипто-приза в результат-пуле free + paid рулеток** (`domain/roulette/`): `RouletteOutcome.lot_id: int | None = None` + invariant `kind == CRYPTO_LOT ⇔ lot_id >= 1` + фабрика `RouletteOutcome.crypto_lot(lot_id=...)`. `pick_roulette_outcome` / `pick_paid_outcome` теперь принимают `active_lots: Sequence[PrizeLot]` вместо `crypto_pool_empty: bool` (`empty` = `len(active_lots) == 0`). Новый helper `_roll_crypto_lot(active_lots, random)` выбирает лот через `random.choice(active_lots)` и возвращает `RouletteOutcome.crypto_lot(lot_id=chosen.id)` (раньше этой ветки не было — при `crypto_pool_empty=False` picker просто возвращал `RouletteOutcome(kind=CRYPTO_LOT)`). Если `active_lots` пуст — picker выкидывает `CRYPTO_LOT` из взвешенного выбора (перетекание веса в LENGTH — как в 4.1-A). Defence-in-depth: если в `active_lots` пришёл non-persisted лот (`id is None`) — `InvalidRouletteConfigError`. Use-case-ы `SpinPaidRoulette` / `SpinFreeRoulette` прокинуты: передают `active_lots=()` на C.5 (реальный `IPrizeLotRepository.list_active(currency)` будет подключён в C.6); audit `ROULETTE_SPIN.after` дополнен полем `lot_id` при `outcome.lot_id is not None`. 25 новых unit-тестов в `tests/unit/domain/roulette/test_crypto_lot_picker.py` (3 валюты, single-lot / multi-lot / empty / non-persisted / mixed-currency / factory shape) + 8 новых в `test_entities.py::TestRouletteOutcomeCryptoLotInvariants`. Существующие `test_picker.py` / `test_paid_picker.py` / `test_spin_free_roulette.py` обновлены под новый параметр + presenter-тесты переведены на `RouletteOutcome(kind=CRYPTO_LOT, lot_id=1)`.
- [ ] **C.6 — Резервирование лота** (`SpinPaidRoulette` / `SpinFreeRoulette`-flow): когда picker возвращает `CryptoLotOutcome(lot_id)`, в той же UoW делать `IPrizeLotRepository.update_status(lot_id, RESERVED)` + audit `PRIZE_LOT_RESERVED`. Если `update_status` упал (race с другим player-ом, выбравшим тот же лот) — fallback на `LengthGain` (или retry picker?). Юнит-тесты на race + happy-path. **Открытое решение:** retry vs fallback.
- [ ] **C.7 — Cron + триггер лот-генератора** (`infrastructure/scheduler/`): cron 1×/час вызывает `GeneratePrizeLots.execute(currency)` для каждой валюты; триггер после крупного донат-зачисления (например, `pool_after_native > prev_balance + threshold`) — отдельный сигнал, обработка в `RecordDonation` или в worker-е. **Открытое решение:** хук в `RecordDonation` vs независимый poll.
- [ ] **C.8 — `IFeeEstimator` имплементация** (`infrastructure/fees/`): на старте `InMemoryFeeEstimator` с константами per currency (P95-аппроксимация); в 4.1-D подменится на `TonRpcFeeEstimator` с реальными запросами. DI-проброс в `Container`.
- [ ] **C.9 — Composition root** (`bot/main.py`): `prize_lot_repo` + `fee_estimator` + `generate_prize_lots` use-case в `Container`; cron-job регистрация. Обновить `_container_with_fakes()` в `test_composition_root.py` с `FakePrizeLotRepository` + `FakeFeeEstimator`.
- [ ] **C.10 — `make ci` локально:** ruff + `mypy --strict` + import-linter (4 contracts kept) + pytest зелёный + coverage gate (≥ 80%).
- [ ] **C.11 — Финальный док-коммит:** `history.md` запись 4.1-C + `current_tasks.md` пересборка под старт **Спринта 4.1-D** «TON Connect + USDT + ClaimPrize».
- [ ] `chore: remove AGENT_HANDOFF before PR` — отдельным коммитом удалить `AGENT_HANDOFF.md`.
- [ ] Открыть PR в `main` по шаблону `.github/pull_request_template.md`.
- [ ] Дождаться зелёного GitHub CI.

---

## 🔗 Что ровно сейчас в работе

> Сюда пиши **дельту** к плану: что именно меняешь, какие use-cases / порты / handler-ы / тесты затронуты.

**Следующий PR — 4.1-C «Лот-генератор + крипто-приз в рулетке»** — третий PR Спринта 4.1 (Фаза 4 «Монетизация и масштаб»). Стартует от свежего `main` после мерджа PR 4.1-B (закрытие призового пула).
- **На `main` (после 4.1-B):** Фаза 3 закрыта полностью (3.1–3.6), Спринт 4.1: A и B закрыты.
- **Скоуп 4.1-C:** domain-агрегат `PrizeLot` + VO `FeeBufferAmount` + порты `IPrizeLotRepository` + `IFeeEstimator` + use-case `GeneratePrizeLots` (нарезка свободного остатка `PrizePool` на лоты `min: 1 USD-эквив + fee_buffer` / `max: 10 USD-эквив`); persistence (`prize_lots` + Alembic `0029` + ORM + repository); audit-source `prize_lot_generated` / `prize_lot_refunded` (Alembic `0030`); picker крипто-приза в результат-пуле free + paid рулеток (выбор активного лота при сработке `crypto_lot`-веса; иначе `LengthGain`-fallback); резервирование лота (`status=ACTIVE → RESERVED`) при выпадении в спин-результате; cron 1×/час + триггер после крупного донат-зачисления; in-memory `IFeeEstimator` (TON RPC — в 4.1-D); composition root. **Без** TON Connect / `ClaimPrize` (4.1-D) / админ-команд (4.1-E).
- **Открытые решения:** retry vs fallback при race-резервировании лота (C.6); хук в `RecordDonation` vs независимый poll для триггера лот-генератора при крупном донат-зачислении (C.7).
- **После мерджа PR 4.1-C:** старт **Спринта 4.1-D** «TON Connect + USDT + ClaimPrize» (задачи 4.1.2, 4.1.3, 4.1.9).

---

## 🛑 Известные блокеры / открытые вопросы PR-а

- **Phase-3 multi-membership ограничение** (наследие 3.6-A). В Phase 3 игрок может состоять максимум в **одном** клане (`UNIQUE` constraint на `clan_members.player_id` ещё с миграции `0006_clan_members`). Поэтому `count_active_for_player` фактически возвращает `0` или `1`, и `tribe_bonus_cm ∈ {0, cm_per_tribe}`. Интерфейс готов к Phase 4+ (multi-membership), но реальный `+131 см cap` будет достигаться только после снятия `UNIQUE`-constraint и реализации UI «вступить в племя × N» (вне скоупа Спринта 4.1).
- **Опциональный hint `oracle-no-tribes-hint`** (3 нулевых `/predict` подряд → «*вступай в новые племена и получай больше см*») — **не реализован** в 3.6-B. Помечен как feature-flag `oracle.show_no_tribes_hint` (default `false`). Реальная реализация требует трекинга последних 3 invocations — отдельный enhancement.
- **`crypto_lot` — реальный розыгрыш — задача 4.1-C.** В 4.1-A/B рулетка вызывалась с `crypto_pool_empty=True` (всегда) → вес `CRYPTO_LOT` перетекал на `LENGTH` в picker-е (free + paid). 4.1-C добавляет `IPrizeLotRepository.list_active(currency)` + picker, который выбирает активный лот при сработке `crypto_lot`-веса. До запуска 4.1-D (`ClaimPrize` + TON SDK) выплата лота приостанавливается на статусе `RESERVED` — игрок видит «лот зарезервирован», получит выплату после привязки кошелька в 4.1-D.
- **Не-LENGTH исходы рулетки (ITEM/SCROLL_REGULAR/SCROLL_BLESSED) — без INSERT в инвентарь.** `RouletteSpinRepository.record(spin)` пишет `kind` + `length_cm=NULL`; audit-payload не содержит `target_id`. Реальный выбор предмета/скролла + INSERT в `items`/`scrolls` — задача отдельного спринта «инвентарь + рулетка интеграция» (после 4.1).
- **`AuditAction.SCROLL_DROP` всё ещё audit-only без write-through в инвентарь** — наследие предыдущих спринтов. Рейды и PvE дропают скроллы только в `audit_log`, без `INSERT` в `scrolls`-таблицу. Запланировано как отдельная задача после Спринта 4.1.
- **Реальный TG Stars-провайдер (skeleton 4.1-A bot-handler).** Skeleton handler принимает `successful_payment` от Telegram, но без подписания payload-а финансовый callback потенциально подделываем. В 4.1-D (TON Connect) добавим signature-верификацию payload-а и серверную сверку (provider-id ⇄ idempotency_key). До этого момента — **только staging/dev**, не запускать в prod.
- **Округление 10%-донат-комиссии:** ГДД §12.6.1 пишет «10% от каждого донат-зачисления → пул», но не уточняет округление при `amount % 10 != 0`. Стартую с `floor-division (// 10)` (= в пользу платформы; пользователь не теряет, потому что увидит точную сумму взимания только на стороне Telegram). Если на review будет фидбек — поменяю на `round half up`.

---

## 📌 Последний коммит на ветке

> Обновляется автоматически перед каждым `git push`. После `git log --oneline -1` — short sha + subject.

Ветка `devin/1778438123-sprint-4-1-C-lot-generator` создана от `main = 93148aa` (merge PR #130). Первый коммит на ветке — **C.0** «`AGENT_HANDOFF.md` + snapshot pivot under 4.1-C». Последний коммит — «feat(4.1-C): C.5 — Picker крипто-приза в free + paid рулетках (RouletteOutcome.crypto_lot(lot_id))».
