# 🍆 Пипирик Варс — Текущие задачи

> Этот файл описывает **только то, что в работе сейчас**: активная feature-ветка, активный спринт/PR, чек-лист текущих шагов и их статусы. По мере выполнения шаги отмечаются `[x]`.
>
> **С 2026-05-08:** обновления `history.md` и `current_tasks.md` под следующий PR делаются **внутри самого фичевого PR** (последним коммитом перед мерджем). Отдельный postmerge-PR больше не открывается — см. [`../CONTRIBUTING.md`](../CONTRIBUTING.md) «Перед мерджем PR-а».
>
> **Длинный план** (фазы / спринты A→Z) — в [`development_plan.md`](development_plan.md).
> **Игровая спецификация** (механики, формулы, баланс) — в [`game_design.md`](game_design.md).
> **Журнал завершённых работ** — в [`history.md`](history.md).
> **Правила работы с документацией + протокол передачи задач между агентами** — в [`../CONTRIBUTING.md`](../CONTRIBUTING.md).
>
> ⚠️ **Перед каждым коммитом** обнови чек-лист ниже (отметь готовые шаги, обнови «текущая позиция»). Это нужно для непрерывности при смене агентов — следующий агент должен знать, где ты остановился.

---

## 📸 Снимок состояния проекта

> Эта секция отражает состояние проекта **на момент последнего обновления этого файла**. Она нужна для того, чтобы новый агент за 30 секунд понял, что происходит. Обновляй её при старте/завершении каждого PR-а.

**На `main` после мерджа PR 4.1-C:** последний смерженный PR — **4.1-C** — Лот-генератор + крипто-приз в рулетке. Domain-агрегат `PrizeLot(id, currency, amount_native, fee_buffer_native, status, created_at, reserved_at|None, claimed_at|None)` + status-машина `PrizeLotStatus.{ACTIVE, RESERVED, CLAIMED, REFUNDED}` (immutable transitions `mark_reserved`/`mark_claimed`/`mark_refunded`/`unreserve`); VO `FeeBufferAmount(int, >= 0)`; порты `IPrizeLotRepository(add, get_by_id, list_active, update_status)` + `IFeeEstimator.estimate_fee(currency, target_amount_native) -> int`. Application use-case `GeneratePrizeLots(currency, idempotency_key) -> GeneratePrizeLotsResult` (нарезает свободный остаток пула на лоты `min: 1 USD-эквив + fee_buffer` / `max: 10 USD-эквив` по ГДД §12.6.3, audit `PRIZE_LOT_GENERATED`, идемпотентно через `prize_lot_generator:<currency>|<key>` namespace, **ambient-UoW-aware** — `is_active`-check: reuse caller-UoW из `RecordDonation`-триггера или открыть свою из cron-callback-а). Picker крипто-приза в `SpinFreeRoulette`/`SpinPaidRoulette` через расширенный `pick_outcome(*, active_lots: Sequence[PrizeLot], ...)` + новая фабрика `RouletteOutcome.crypto_lot(lot_id)` + инвариант `lot_id >= 1 IFF kind == CRYPTO_LOT`; при выпадении `CRYPTO_LOT` use-case вызывает `update_status(lot_id, RESERVED)` + audit `PRIZE_LOT_RESERVED`; race-fallback на `PrizeLotStatusTransitionError` → `pick_length_only_outcome` (MVP-стратегия: сразу LengthGain без retry-loop). Persistence: таблица `prize_lots` + Alembic `0030_prize_lots` + `PrizeLotORM` + `SqlAlchemyPrizeLotRepository`. Alembic-миграции audit-sources: `0029_audit_source_prize_lot_generated`, `0031_audit_source_prize_lot_refunded` (без write-сайтов — на 4.1-D `ClaimPrize`-таймауты), `0032_audit_source_prize_lot_reserved`. Infrastructure: `InMemoryFeeEstimator` (константный `IFeeEstimator`-адаптер: STARS=0, TON_NANO=10_000_000, USDT_DECIMAL=200_000 — в 4.1-D заменится `TonRpcFeeEstimator`-ом с P95 за 7 дней); hourly cron `GeneratePrizeLots` per currency через `APSchedulerDelayedJobScheduler.schedule_prize_lot_generator_cron()` (cron-id `prize_lot_generator_cron:<currency>`, `IntervalTrigger(hours=1)`, идемпотентен через `prize_lot_generator:cron:<currency>:<period_id>`) + внеочередной триггер из `RecordDonation` при `donation_native >= _DONATION_TRIGGER_THRESHOLD[currency]` (TON_NANO=`500_000_000` ≈ 0.5 TON, USDT_DECIMAL=`1_000_000` ≈ 1 USDT, STARS=`None` — без триггера). Composition root прокинул `prize_lot_repo`/`fee_estimator`/`generate_prize_lots`/`record_donation` + DI `prize_lots: IPrizeLotRepository` в `SpinFreeRoulette`/`SpinPaidRoulette`. **Без** TON Connect / USDT-провайдера / `ClaimPrize` / `TonRpcFeeEstimator` / админ-команд — это следующие PR-ы (4.1-D…F). Перед `4.1-C`: **4.1-B** (PR #130, `93148aa`) — Призовой пул + persistence + audit (domain `PrizePool` + VO + port + use-case `RecordDonation` (10% от платежа) + Alembic `0027`/`0028` + audit `PRIZE_POOL_INCREMENT` + интеграция в Step 5b `SpinPaidRoulette`-flow); Перед `4.1-B`: **CI cost-cut** (PR #129, `da7100a`) — pytest-xdist + py3.12-only + paths-ignore docs (≈52% экономии CI-минут); **4.1-A** (PR #128, `21c21c0`) — Telegram Stars skeleton; **3.6-B** (PR #127, `b684679`); **3.6-A** (PR #126); **3.5-D** (PR #125); **3.5-A→C** (#121–#123); **fix(load-tests)** (#124); **3.4-A→D** (#117–#120); **3.6 design doc** (#116); **3.3-A→D** (#112–#115); **3.2-A→D** (#108–#111); **3.1-A→E** (#99–#107). **Закрыты Спринты 3.1–3.6** (Фаза 3) + **Спринт 4.1, PR-ы A, B и C** (Фаза 4). **В работе Фаза 4 «Монетизация и масштаб»** ([`development_plan.md`](development_plan.md) §7) — **Спринт 4.1**, следующий PR **4.1-D «TON Connect + USDT + ClaimPrize»**.

**Текущая ветка** — будет создана от `main` после мерджа PR 4.1-C — `devin/<timestamp>-sprint-4-1-D-ton-connect-usdt-claim-prize` под **Спринт 4.1-D «TON Connect + USDT + ClaimPrize»** (четвёртый PR Спринта 4.1). В PR 4.1-C закрыты: C.0 (snapshot pivot + sticky HANDOFF), C.1 (Domain `PrizeLot` aggregate + VO `FeeBufferAmount` + ports `IPrizeLotRepository` / `IFeeEstimator` + errors + 67 unit-тестов), C.2 (Application use-case `GeneratePrizeLots` + audit-source `PRIZE_LOT_GENERATED` + Alembic `0029` + 41 unit-тест), C.3 (Alembic `0030_prize_lots` + `PrizeLotORM` + `SqlAlchemyPrizeLotRepository` + 33 integration-теста), C.4 (audit-source `PRIZE_LOT_REFUNDED` + Alembic `0031`), C.5 (picker `active_lots: Sequence[PrizeLot]` + `RouletteOutcome.crypto_lot(lot_id)`-фабрика + invariants `lot_id >= 1` IFF `CRYPTO_LOT` + 25 новых unit-тестов), C.6.a (audit `PRIZE_LOT_RESERVED` + Alembic `0032`), C.6.b (DI `prize_lots: IPrizeLotRepository` в use-case-ы спинов + composition root), C.6.c (happy-path резервирования `ACTIVE → RESERVED` + audit), C.6.d (race-fallback `PrizeLotStatusTransitionError` → LengthGain via `pick_length_only_outcome`), C.6.e (integration-тест резервирования с SqlAlchemy + ORM whitelist sync), C.7.a (`InMemoryFeeEstimator` константный + 8 unit-тестов), C.7.b (hourly cron-entry `GeneratePrizeLots` per currency в `aps.py` + `GeneratePrizeLots`-инстанс в `bot/main.py::Container` с `InMemoryFeeEstimator()` + 8 unit-тестов `TestPrizeLotGeneratorCron`), C.7.c (smoke-тест cron-flow с реальным prod-стеком + 3 тест-кейса), C.7.c.fix (mypy fix в `_FakeGeneratePrizeLotsUseCase.commands`), **C.7.d (этот коммит)** — триггер `GeneratePrizeLots.execute(...)` внутри `RecordDonation.execute(...)` (вариант А) для крупных донатов TON_NANO (`>= 500_000_000`) / USDT_DECIMAL (`>= 1_000_000`); STARS — без триггера. `GeneratePrizeLots` теперь ambient-UoW-aware (`is_active`-check), DI-параметр `generate_prize_lots: GeneratePrizeLots` в `RecordDonation.__init__`, проброс через composition root. 7 unit-тестов `TestPrizeLotGeneratorTrigger`.

**Roadmap (Спринт 4.1, декомпозиция на фичевые PR-ы):**
- **4.1-A — Telegram Stars + платная рулетка (skeleton)** (задачи 4.1.1, 4.1.4) — **закрыт PR #128**: domain-payments + paid roulette + use-case `SpinPaidRoulette` + порт `IPaymentLedger` + persistence + audit + конфиг + bot-handler skeleton + 26 локалей × RU+EN.
- **4.1-B — Призовой пул + persistence + audit** (задачи 4.1.5, 4.1.6) — **закрыт PR #130**: domain-агрегат `PrizePool(stars, ton_nano, usdt_decimal)` + VO + `IPrizePoolRepository` + use-case `RecordDonation` (10% → пул, идемпотентно) + persistence (`prize_pool_balance` + Alembic `0027` + ORM + repository) + audit-source `PRIZE_POOL_INCREMENT` (Alembic `0028`) + интеграция в `SpinPaidRoulette`-flow (Step 5b) + composition root.
- **4.1-C — Лот-генератор + крипто-приз в рулетке** (задачи 4.1.7, 4.1.8) — **закрыт PR 4.1-C**: domain `PrizeLot` aggregate + status-machine + VO `FeeBufferAmount` + ports `IPrizeLotRepository`/`IFeeEstimator`; use-case `GeneratePrizeLots` (ambient-UoW-aware) + Alembic `0029_audit_source_prize_lot_generated`; persistence (`prize_lots` + Alembic `0030_prize_lots` + ORM + repository); audit-sources `PRIZE_LOT_REFUNDED` (Alembic `0031`) + `PRIZE_LOT_RESERVED` (Alembic `0032`); picker крипто-приза + резервирование лота `ACTIVE → RESERVED` + race-fallback на LengthGain; `InMemoryFeeEstimator` (константный); hourly cron `GeneratePrizeLots` per currency + триггер после крупного донат-зачисления (`RecordDonation`-вариант (А) с ambient-UoW); composition root.
- **4.1-D — TON Connect + USDT + ClaimPrize** (задачи 4.1.2, 4.1.3, 4.1.9) — **активный**: TON SDK, USDT через TON-jetton-процессор, `ClaimPrize`, handler «Привязать кошелёк», integration в sandbox; signature-верификация TG Stars payload-а (вывод 4.1-A handler-а в продакшн); `TonRpcFeeEstimator` (заменяет `InMemoryFeeEstimator`); refund-таймауты RESERVED-лотов (на `ClaimPrize`-тайм-аут писать audit `PRIZE_LOT_REFUNDED` + `update_status(RESERVED → ACTIVE)`).
- **4.1-E — Админ-команды + лимиты выплат** (задачи 4.1.10, 4.1.11): `/prize_pool`/`/refund_lot`/`/freeze_payouts` (super_admin + TOTP), rolling-30d-лимит.
- **4.1-F — Redis + ИИ + многоязычность + метрики + закрытие Спринта 4.1** (задачи 4.1.12, 4.1.13, 4.1.14, 4.1.15): переход на Redis, опц. ИИ-предсказания, +6 локалей (PT, ES, TR, ID, FA, UK), Prometheus + Grafana, финальный док-коммит.

---

## 🎯 Активный спринт — Спринт 4.1 «Монетизация и масштаб» 🎯 (Фаза 4)

> Цель спринта (по [`development_plan.md`](development_plan.md) §7 «Фаза 4 — Монетизация и масштаб», ГДД §12.5/§12.6): запуск платных каналов (Telegram Stars / TON / USDT) + крипто-призовой пул из 10% донат-зачислений + лот-генератор + крипто-приз в рулетке + use-case `ClaimPrize` + админ-команды + переход на Redis + многоязычность + метрики. Декомпозиция на 6 фичевых PR-ов (4.1-A → 4.1-F, см. Roadmap выше).

**Скоуп — задачи плана 4.1.* (детали — в [`development_plan.md`](development_plan.md) §7):**

- **4.1.1 — Telegram Stars: платная рулетка** ✅ — реализовано в 4.1-A (PR #128). Skeleton: invoice + pre_checkout_query + successful_payment → `SpinPaidRoulette`. До 4.1-D — staging-only.
- **4.1.2 — TON Connect:** фикс длина за TON. Sandbox + продакшн-сеть; webhook/poll платежей. (4.1-D)
- **4.1.3 — USDT (через TON-сеть/процессор):** параметризованные суммы → длина. (4.1-D)
- **4.1.4 — Антифрод платежей,** проверка двойных зачислений ✅ — реализовано в 4.1-A (PR #128) — `IdempotencyKey` VO + DB `UNIQUE (player_id, idempotency_key)` + on-conflict + anti-fraud-сверка currency/amount.
- **4.1.5 — 10% от каждого донат-зачисления → крипто-призовой пул** (ГДД §12.6) ✅ — реализовано в 4.1-B: `RecordDonation` use-case при подтверждении платежа делает второй проводкой `apply_increment(currency, donation = payment_amount_native // 10)`. Идемпотентно через scope `:prize_pool` поверх root-`idempotency_key`-а платежа.
- **4.1.6 — Призовой пул** ✅ — реализовано в 4.1-B: domain-агрегат `PrizePool(stars, ton_nano, usdt_decimal)`, persistence (`prize_pool_balance`), Alembic `0027`, audit `PRIZE_POOL_INCREMENT` (`0028`).
- **4.1.7 — Лот-генератор** (`GeneratePrizeLots`, ГДД §12.6.3) ✅ — реализовано в 4.1-C: domain `PrizeLot`-агрегат + status-машина, application use-case `GeneratePrizeLots` (нарезка свободного остатка пула с учётом `IFeeEstimator`-буфера), `InMemoryFeeEstimator` (константный, в 4.1-D → `TonRpcFeeEstimator` с P95 за 7 дней), hourly cron + триггер при крупном донат-зачислении, persistence (`prize_lots` + Alembic `0030`), audit `PRIZE_LOT_GENERATED`/`PRIZE_LOT_RESERVED`/`PRIZE_LOT_REFUNDED`.
- **4.1.8 — Крипто-приз** в результат-пуле платной + free-рулеток (ГДД §12.4.2, §12.5.2) ✅ — реализовано в 4.1-C: picker `active_lots: Sequence[PrizeLot]` + `RouletteOutcome.crypto_lot(lot_id)`-фабрика; резервирование лота `ACTIVE → RESERVED` + audit при выпадении в спин-результате; race-fallback на LengthGain при `PrizeLotStatusTransitionError`. Выплата зарезервированного лота отложена до `ClaimPrize` в 4.1-D.
- **4.1.9 — Выплата выигрыша:** handler «Привязать кошелёк» + use-case `ClaimPrize(player, lot_id, recipient_address)` + транзакция через TON SDK. Жёсткая защита: `actual_fee > fee_buffer` → лот возвращается в пул, выплата откладывается. Юнит-тесты на все ветки; integration в TON sandbox. (4.1-D)
- **4.1.10 — Админ-команды** `/prize_pool`, `/refund_lot <lot_id>`, `/freeze_payouts` (super_admin + TOTP, ГДД §12.6.6). RBAC-тесты; audit-записи `ADMIN_PRIZE_*`. (4.1-E)
- **4.1.11 — Лимиты выплат на игрока:** `max 50 USDT-экв за 30 дней` (TODO(balance): финальное число). Сверх лимита — выплата ставится в очередь. Юнит-тесты на rolling 30 day window. (4.1-E)
- **4.1.12 — Переход на Redis** (лобби, очереди, DAU, locks). Нагрузочный тест 10× от MVP. (4.1-F)
- **4.1.13 — Перевод предсказаний/логов на ИИ** (опционально). Кэш на сгенерированных ответах. (4.1-F)
- **4.1.14 — Доп. языки:** PT, ES, TR, ID, FA, UK. Файлы переводов, тест fallback. (4.1-F)
- **4.1.15 — Метрики и дашборд** (Prometheus + Grafana). Графики DAU/RPS/караваны/рейды + крипто-пул per currency. (4.1-F)

**Финальный коммит каждого PR-а Спринта 4.1** (внутри ветки, последним перед мерджем) — обновить `history.md` (запись «Спринт 4.1-X: ...») + пересобрать «Снимок состояния» в `current_tasks.md` под `main = <коммит_слияния>`, передвинуть чек-лист на следующий PR (или закрыть Спринт 4.1 на 4.1-F и расписать чек-лист **первого PR-а Спринта 4.2**, если он спланирован).

---

## 📝 Чек-лист текущего PR — 4.1-D «TON Connect + USDT + ClaimPrize»

> Скоуп (гипотеза декомпозиции — следующий агент пусть пересмотрит): задачи плана 4.1.2 (TON Connect: фикс-длина за TON), 4.1.3 (USDT через TON-jetton-процессор), 4.1.9 (handler «Привязать кошелёк» + use-case `ClaimPrize(player, lot_id, recipient_address)` + транзакция через TON SDK + жёсткая защита `actual_fee > fee_buffer` → refund-в-пул). Плюс долги 4.1-C: `TonRpcFeeEstimator` (заменяет `InMemoryFeeEstimator`); refund-таймауты `RESERVED`-лотов (на `ClaimPrize`-тайм-аут писать audit `PRIZE_LOT_REFUNDED` + `update_status(RESERVED → ACTIVE)`); signature-верификация TG Stars payload-а (4.1-A skeleton handler — выводим в продакшн).

- [x] `git fetch && git checkout main && git pull` (свежий `main = db8e630`, merge PR #131 4.1-C).
- [x] Создать ветку `devin/1778501374-sprint-4-1-D-ton-connect-usdt-claim-prize` от свежего `main`.
- [x] **D.0 — Snapshot pivot + sticky `AGENT_HANDOFF.md`** под старт Спринта 4.1-D: пересобрать «Снимок состояния» под `main = <merge-of-4.1-C>`, переписать чек-лист D.0–D.N под фактическую декомпозицию шагов после ревью гипотезы, создать `AGENT_HANDOFF.md` (sticky-режим).
- [x] **D.1 — Domain Wallet + VO + ports** (`src/pipirik_wars/domain/monetization/`): `Wallet(player_id, address, currency)` (frozen+slots) + VO `TonAddress` / `UsdtJettonAddress` + порт `IWalletRepository` + ошибки `WalletNotLinkedError` / `WalletAlreadyLinkedError`. Unit-тесты (37).
- [x] **D.2 — Application use-case `ClaimPrize`** (`src/pipirik_wars/application/monetization/claim_prize.py`): принимает `(player_id, lot_id, recipient_address) -> ClaimPrizeResult`; проверяет `wallet.address == recipient_address` (anti-fraud); вызывает `ITonPayoutAdapter.payout(...)`; на success — `update_status(lot_id, CLAIMED)` + audit `PRIZE_LOT_CLAIMED`; на `actual_fee > fee_buffer` — `update_status(lot_id, REFUNDED)` + `apply_increment(currency, amount)` + audit `PRIZE_LOT_REFUNDED`. Идемпотентно. Audit-source `PRIZE_LOT_CLAIMED` + Alembic 0033/0034. Unit-тесты (6).
- [x] **D.3 — Application use-case `LinkWallet`** (`application/monetization/link_wallet.py`): принимает `(player_id, address, currency, signature) -> LinkWalletResult`; верифицирует TON-Connect-подпись через `ITonConnectVerifier`; `wallet_repo.add_or_replace(...)`; audit `WALLET_LINKED`. Unit-тесты (4).
- [x] **D.4 — Persistence `wallets`**: Alembic 0035 + `WalletORM` + `SqlAlchemyWalletRepository` (upsert через ON CONFLICT для Postgres+SQLite). Integration-тесты (6).
- [x] **D.5 — TON-сеть adapter** (`infrastructure/payments/ton_rpc/`): `TonRpcAdapter` (`ITonPayoutAdapter`) + `TonRpcFeeEstimator` (`IFeeEstimator`, nearest-rank P95 за 7 дней) + `JettonUsdtProvider` (resolve `get_wallet_address` + TEP-74 transfer payload) + `TonRpcSettings` (pydantic-settings `TON_RPC_*`, SecretStr api_key). Тесты на `FakeTonRpcClient` (56 unit). Сборка signed-BOC — текстовый стаб до D.10 (реальный TEP-67/74 BOC + Ed25519-подпись и HTTP-имплементация `ITonRpcClient` подключаются на D.10 вместе с composition root).
- [x] **D.6 — Bot-handler «Привязать кошелёк»** (`src/pipirik_wars/bot/`): `/link_wallet` (личка-only) → prompt-карточка с inline-клавиатурой выбора валюты TON / USDT + callback `link_wallet:select:<ton|usdt>` → локализованные инструкции по подписи кошелька через TON-Connect-совместимый клиент. Backend-вход `/link_wallet_confirm <currency> <address> <proof>` → `LinkWallet.execute(...)`, ветки `linked` / `relinked` / `already-linked` / `invalid-proof` / `unsupported` / `not-registered` + 20 ключей `link-wallet-*` в обеих локалях + 54 unit-теста.
- [ ] **D.7 — Bot-handler `/claim_prize <lot_id>`** + presenter «ваш лот зарезервирован, нажмите чтобы вывести» в результатах рулетки; on-success — выплата проведена, on-refund — лот возвращён в пул. **Декомпозиция под уменьшенные лимиты (2026-05-11):**
  - [x] **D.7.a — `ClaimPrizePresenter` + callback_data** (`src/pipirik_wars/bot/presenters/claim_prize.py`): `claim_prize_callback_data` / `parse_claim_prize_callback_data` (формат `claim_prize:<lot_id>`, ≤ 64 байта), presenter-методы prompt («лот зарезервирован») / success / refund / not-found / already-claimed / not-reserved / wallet-not-linked / not-owner / invalid-callback + 20 ключей `claim-prize-*` в RU/EN-локалях + 33 presenter-теста.
  - [x] **D.7.b — `/claim_prize <lot_id>` handler** (`src/pipirik_wars/bot/handlers/claim_prize.py`): личка-only handler, парсинг `lot_id` → pre-load `IPrizeLotRepository.get_by_id(...)` (ветки `not-found` / `already-claimed` / `not-reserved`) → pre-load `IWalletRepository.get_by_player_and_currency(...)` (ветка `wallet-not-linked`) → `ClaimPrize.execute(...)` → ветки `success` / `refund`. Race-condition: ловим `PrizeLotNotFoundError` / `PrizeLotStatusTransitionError` / `WalletNotLinkedError` от use-case и рендерим соответствующие ветки. 23 handler-теста (tests/unit/bot/handlers/test_claim_prize.py). Refund-ветка пока с placeholder `actual_fee_native=0` (полный контракт `ClaimPrizeResult` — на D.10.c).
  - [x] **D.7.c — Кнопка «Забрать приз» в результате рулетки + callback handler** — callback `claim_prize:<lot_id>` handler в claim_prize.py (парсит callback_data, снимает клавиатуру, делегирует в `_process_claim`). Roulette handlers (free + paid) при `CRYPTO_LOT`-исходе прикрепляют inline-кнопку через `build_claim_prize_keyboard(...)`. `ClaimPrizePresenter.button_text(locale)` + `build_claim_prize_keyboard_multi(lot_ids)` для PACK_10. 29 handler-тестов (23 message + 5 callback + 1 keyboard).
  - [x] **D.7.d — Регистрация `claim_prize_router`** в `src/pipirik_wars/bot/handlers/__init__.py` (после `roulette_paid_router`) + 3 smoke-теста в `tests/unit/bot/handlers/test_register_routers.py` (router зарегистрирован, link_wallet тоже, порядок claim_prize после roulette_paid).
- [ ] **D.8 — TG Stars payload signature verification** (вывод 4.1-A handler-а в продакшн): добавить server-side подпись payload-а + сверка `provider_id ⇄ idempotency_key`. **Декомпозиция под уменьшенные лимиты (2026-05-11):**
  - [x] **D.8.a — Domain VO `StarsPayload(pack_value, idempotency_seed)` + port `ITgStarsPayloadVerifier(verify) -> StarsPayload`** (sync, HMAC-без-I/O; в сигнатуре `currency: Currency` — расширение на будущие валюты) + ошибка `InvalidStarsPayloadError(reason, payload_len)` (machine-readable без самого payload-а) + ре-экспорт в `domain/monetization/__init__.py` + 43 unit-теста (`tests/unit/domain/monetization/test_stars_payload.py`).
  - [x] **D.8.b — Infrastructure `HmacTgStarsPayloadVerifier`** (`src/pipirik_wars/infrastructure/payments/tg_stars/verifier.py`): реализация порта `ITgStarsPayloadVerifier` поверх HMAC-SHA256. Payload-формат v1: `<version>:<pack_value>:<seed>:<hmac_b64url>`, HMAC покрывает `version|pack_value|seed|amount_native|currency` (NUL-разделённый контекст; `provider_payment_id` в HMAC не входит, потому что не известен на момент выпуска invoice-а, вместо этого non-empty-проверка — `bad_provider_id`). Метод-пара `serialize(...)` / `verify(...)`; `hmac.compare_digest` (constant-time). `TgStarsSettings` (pydantic-settings prefix `TG_STARS_`, обязательный `secret: SecretStr` — fail-closed без env). 51 unit-тест (11 settings + 40 verifier: format, golden-HMAC, round-trip, 12 failure-modes, port-conformance).
  - [x] **D.8.c — Wire в `successful_payment`-handler 4.1-A**: в `bot/handlers/roulette_paid.py` (`handle_roulette_paid_buy` + `handle_successful_payment`) DI-проброс `tg_stars_verifier: ITgStarsPayloadVerifier`; `serialize(...)` при выдаче invoice-а со свежим `secrets.token_urlsafe(18)` seed-ом; `verify(...)` **перед** `SpinPaidRoulette.execute(...)`; на `InvalidStarsPayloadError` — structured-log `WARNING(reason, payload_len, tg_payment_charge_id)` + локализованная карточка `payment_invalid`. Port-расширение: в `ITgStarsPayloadVerifier` добавлен метод `serialize(...)`. `parse_invoice_payload(...)` теперь принимает оба формата (v0 legacy + v1 signed; HMAC верифицируется только в `verify(...)`, не в парсере). 6 новых handler-тестов (idempotency_seed fresh-per-call + 4 ветки `InvalidStarsPayloadError` с caplog-ассертами + legacy-v0 reject) + 2 presenter-теста + 4 теста для v1-format `parse_invoice_payload`; `FakeTgStarsPayloadVerifier` в `tests/fakes/`. `make ci` зелёный (6014 passed, 95.63% cov).
- [ ] **D.9 — Refund-таймаут `RESERVED → ACTIVE`** (долг 4.1-C): cron-job, который перебирает `prize_lots WHERE status = 'reserved' AND reserved_at < now() - lot_ttl_seconds` → `update_status(RESERVED → ACTIVE)` + `apply_increment(currency, amount)` обратно в пул + audit `PRIZE_LOT_REFUNDED`. **Декомпозиция под уменьшенные лимиты (2026-05-11):**
  - [ ] **D.9.a — Balance-секция `prize_lot.reserved_ttl_seconds`** (`config/balance.yaml`) + Pydantic-схема в `infrastructure/balance/schema.py` + loader integration + hot-reload-тест.
  - [ ] **D.9.b — `IPrizeLotRepository.list_expired_reserved(now, ttl_seconds)`** в порт + `SqlAlchemyPrizeLotRepository`-имплементация + 4+ integration-тестов.
  - [ ] **D.9.c — Use-case `ExpireReservedPrizeLots(currency, now) -> ExpireReservedPrizeLotsResult`**: транзакционно `update_status(RESERVED → ACTIVE)` + `apply_increment(...)` + audit `PRIZE_LOT_REFUNDED`. Идемпотентно через `expire_reserved:<currency>|<lot_id>` namespace. 10+ unit-тестов.
  - [ ] **D.9.d — Cron-entry `expire_reserved_prize_lots`** per currency в `infrastructure/scheduler/aps.py` + DI в `bot/main.py::Container` + 5+ unit-тестов.
- [ ] **D.10 — Composition root + real TON-client + real BOC**. **Декомпозиция под уменьшенные лимиты (2026-05-11):**
  - [ ] **D.10.a — Real `TonRpcHttpClient(httpx)`** (`infrastructure/payments/ton_rpc/http_client.py`), реализует `ITonRpcClient.run_get_method` / `send_boc` / `recent_fees` через TON Center API + 12+ моков (без сетевых вызовов).
  - [ ] **D.10.b — Real `TonRpcAdapter._build_*_boc(...)`**: TEP-67/TEP-74-сериализация + Ed25519-подпись (использовать `tonpy`/`pynacl`) + `query_id` через `hashlib.blake2b(idempotency_key)` + 12+ unit-тестов на golden BOC.
  - [ ] **D.10.c — Composition root в `bot/main.py::Container`**: подключить `wallet_repo`, `ton_rpc_adapter` (`ITonPayoutAdapter`), `ton_rpc_fee_estimator` (заменить `InMemoryFeeEstimator`), `link_wallet`-use-case, `claim_prize`-use-case, `expire_reserved_prize_lots`-cron + 4-6 DI-тестов.
  - [ ] **D.10.d — Smoke-test composition root**: bot startup с mocked TON-RPC (без живой сети) + 3 кейса (link_wallet, claim_prize-success, claim_prize-refund).
- [ ] **D.11 — Локальный `make ci` зелёный** + `pre-commit run --all-files`.
- [ ] **D.12 — Финальный док-коммит**: `docs/history.md` (запись «Спринт 4.1-D: ...») + пересобрать «Снимок состояния» в `current_tasks.md` под `main = <merge-of-4.1-D>` + чек-лист 4.1-E.
- [ ] **D.13 — `chore: remove AGENT_HANDOFF before PR`** — отдельным коммитом удалить `AGENT_HANDOFF.md`.
- [ ] **D.14 — Открыть PR** в `main` по шаблону.
- [ ] **D.15 — Дождаться зелёного GitHub CI** через `git pr_checks(...)`.

---

## 🔗 Что ровно сейчас в работе

> Сюда пиши **дельту** к плану: что именно меняешь, какие use-cases / порты / handler-ы / тесты затронуты.

**Активный PR — 4.1-D «TON Connect + USDT + ClaimPrize»** — четвёртый PR Спринта 4.1 (Фаза 4 «Монетизация и масштаб»).
- **Ветка:** `devin/1778501374-sprint-4-1-D-ton-connect-usdt-claim-prize` от `main` = `db8e630` (merge PR #131 4.1-C).
- **На `main` (после 4.1-C):** Фаза 3 закрыта полностью (3.1–3.6), Спринт 4.1: A, B и C закрыты.
- **Текущая позиция:** D.0 → D.7 + D.8.a + D.8.b + D.8.c завершены (Domain Wallet/StarsPayload + ports / VO; use-cases `ClaimPrize` + `LinkWallet`; persistence `wallets` через Alembic 0033/0034/0035 + ORM + repository; infrastructure TON-RPC адаптеры `TonRpcAdapter` + `TonRpcFeeEstimator` + `JettonUsdtProvider` + `TonRpcSettings` с тестами на `FakeTonRpcClient`; bot-handlers `/link_wallet` + `/link_wallet_confirm` + presenter + RU/EN-локали + 54 unit-теста; `ClaimPrizePresenter` + `/claim_prize` + callback `claim_prize:<lot_id>` + claim_prize_router + 88 тестов; доменный порт `ITgStarsPayloadVerifier` + VO `StarsPayload` + ошибка `InvalidStarsPayloadError` + 43 теста; infrastructure `HmacTgStarsPayloadVerifier` + `TgStarsSettings` + 51 тест; D.8.c — wire верификатора в `roulette_paid.py` (serialize перед send_invoice + verify перед SpinPaidRoulette + payment_invalid-карточка + 12 новых тестов + `FakeTgStarsPayloadVerifier`)). `make ci` локально зелёный на текущем коммите D.8.c (6014 passed, 2 skipped, 95.63% coverage). Последний коммит (в работе): `feat(4.1-D): D.8.c — Wire ITgStarsPayloadVerifier into roulette_paid.py (serialize + verify) + 6 unit-тестов`. Следующий — **D.9.a** (Balance-секция `prize_lot.reserved_ttl_seconds`).
- **Передача работы 2026-05-11.** Принял ветку у предыдущего агента (Devin-сессия `023913aca93949be811f82a26708897e`). Выполнил 7 шагов приёмки по CONTRIBUTING.md (HANDOFF → git fetch → доки → `make ci` зелёный на `607feb0` → sticky-обновление HANDOFF → current_tasks.md → D.8.a). Декомпозиция D.7–D.10 на 14 микрошагов (D.7.a-d, D.8.a-c, D.9.a-d, D.10.a-d) унаследована от предыдущего агента (введена 2026-05-11 под уменьшенные лимиты агентских токенов).
- **Передача работы 2026-05-11 (новый агент).** Очередной агент принял ветку (Devin-сессия `70aa083697024881a07c16048bb2e909`). Выполнил 7 шагов приёмки (HANDOFF → git fetch → доки → `make ci` зелёный 6002 passed, 95.64% cov на `ede95dd` → sticky-обновление HANDOFF → current_tasks.md → D.8.c). В этом же коммите — D.8.c.
- **Скоуп 4.1-D:** domain `Wallet` + VO `TonAddress`/`UsdtJettonAddress` + порт `IWalletRepository`; application use-case `LinkWallet` (TON Connect signature verify) + `ClaimPrize` (выплата зарезервированного лота через TON SDK с anti-fraud-проверкой `recipient_address` и refund-в-пул при `actual_fee > fee_buffer`); persistence `wallets` + Alembic; infrastructure `TonRpcAdapter` + `TonRpcFeeEstimator` (P95 газа за 7 дней, заменяет `InMemoryFeeEstimator`); jetton-USDT-провайдер; bot-handlers `/link_wallet` + `/claim_prize <lot_id>`; signature-верификация TG Stars payload-а (вывод 4.1-A handler-а в продакшн); refund-таймауты `RESERVED → ACTIVE` (cron/поллер по `lot_ttl_seconds`); composition root.
- **Открытые решения:** `lot_ttl_seconds` для `RESERVED`-таймаута; формат `payload_json` для audit `PRIZE_LOT_CLAIMED`/`PRIZE_LOT_REFUNDED`; ORM-CHECK whitelist-sync test guard (см. C.6.e — backlog).
- **После мерджа PR 4.1-D:** старт **Спринта 4.1-E** «Админ-команды + лимиты выплат» (задачи 4.1.10, 4.1.11).

---

## 🛑 Известные блокеры / открытые вопросы PR-а

- **Phase-3 multi-membership ограничение** (наследие 3.6-A). В Phase 3 игрок может состоять максимум в **одном** клане (`UNIQUE` constraint на `clan_members.player_id` ещё с миграции `0006_clan_members`). Поэтому `count_active_for_player` фактически возвращает `0` или `1`, и `tribe_bonus_cm ∈ {0, cm_per_tribe}`. Интерфейс готов к Phase 4+ (multi-membership), но реальный `+131 см cap` будет достигаться только после снятия `UNIQUE`-constraint и реализации UI «вступить в племя × N» (вне скоупа Спринта 4.1).
- **Опциональный hint `oracle-no-tribes-hint`** (3 нулевых `/predict` подряд → «*вступай в новые племена и получай больше см*») — **не реализован** в 3.6-B. Помечен как feature-flag `oracle.show_no_tribes_hint` (default `false`). Реальная реализация требует трекинга последних 3 invocations — отдельный enhancement.
- **`ClaimPrize` — выплата зарезервированных лотов — задача 4.1-D.** В 4.1-C реализован picker `crypto_lot` (выбор активного лота при сработке) + резервирование `ACTIVE → RESERVED` + race-fallback на LengthGain. Но реальная выплата TON/USDT-лотов отсутствует — лоты висят в статусе `RESERVED` неопределённо долго. 4.1-D добавит use-case `ClaimPrize(player, lot_id, recipient_address)` + handler «Привязать кошелёк» (TON Connect) + транзакцию через TON SDK; refund-таймаут (`RESERVED → ACTIVE` через `lot_ttl_seconds`-cron + audit `PRIZE_LOT_REFUNDED`); жёсткая защита `actual_fee > fee_buffer` → refund-в-пул.
- **`InMemoryFeeEstimator` — заменяется в 4.1-D.** В 4.1-C использован константный `IFeeEstimator`-адаптер (STARS=0, TON_NANO=10_000_000 ≈ 0.01 TON, USDT_DECIMAL=200_000 ≈ 0.2 USDT-decimal — буфер на TON-газ jetton-перевода). На реальном TON sandbox-е fee-буфер должен опираться на P95 за 7 дней через `TonRpcFeeEstimator` (заменит `InMemoryFeeEstimator` через тот же `IFeeEstimator`-порт в `bot/main.py::Container`).
- **`ORM.__table_args__`-CHECK дрейф (см. C.6.e).** `AuditLogORM.__table_args__` `audit_log_source_whitelist`-CHECK устарел относительно Alembic-миграций; пере-синхронизирован вручную в C.6.e. Backlog: автоматизировать «не забыть синхронизировать ORM-CHECK с Alembic при добавлении audit-source» через test-страж (например, `test_orm_check_matches_latest_migration`).
- **Не-LENGTH исходы рулетки (ITEM/SCROLL_REGULAR/SCROLL_BLESSED) — без INSERT в инвентарь.** `RouletteSpinRepository.record(spin)` пишет `kind` + `length_cm=NULL`; audit-payload не содержит `target_id`. Реальный выбор предмета/скролла + INSERT в `items`/`scrolls` — задача отдельного спринта «инвентарь + рулетка интеграция» (после 4.1).
- **`AuditAction.SCROLL_DROP` всё ещё audit-only без write-through в инвентарь** — наследие предыдущих спринтов. Рейды и PvE дропают скроллы только в `audit_log`, без `INSERT` в `scrolls`-таблицу. Запланировано как отдельная задача после Спринта 4.1.
- **Реальный TG Stars-провайдер (skeleton 4.1-A bot-handler).** Skeleton handler принимает `successful_payment` от Telegram, но без подписания payload-а финансовый callback потенциально подделываем. В 4.1-D (TON Connect) добавим signature-верификацию payload-а и серверную сверку (provider-id ⇄ idempotency_key). До этого момента — **только staging/dev**, не запускать в prod.
- **Округление 10%-донат-комиссии:** ГДД §12.6.1 пишет «10% от каждого донат-зачисления → пул», но не уточняет округление при `amount % 10 != 0`. Стартую с `floor-division (// 10)` (= в пользу платформы; пользователь не теряет, потому что увидит точную сумму взимания только на стороне Telegram). Если на review будет фидбек — поменяю на `round half up`.

---

## 📌 Последний коммит на ветке

> Обновляется автоматически перед каждым `git push`. После `git log --oneline -1` — short sha + subject.

Ветка `devin/1778501374-sprint-4-1-D-ton-connect-usdt-claim-prize` создана от `main = db8e630` (merge PR #131 4.1-C). Первый коммит на ветке — **D.0** «snapshot pivot + sticky `AGENT_HANDOFF.md` под 4.1-D». Последний коммит на ветке — `feat(4.1-D): D.6 — Bot-handler /link_wallet + TON Connect deeplink + callback + 54 unit-тестов` (`0f312da`). После приёмки нового агента 2026-05-11 чек-лист D.7–D.15 разбит на 14 микрошагов; sticky `AGENT_HANDOFF.md` обновлён под нового агента в этом же docs-коммите.
