"""Domain-слой: чистая бизнес-логика.

Содержит сущности (entities), value objects и доменные сервисы.
Зависит ТОЛЬКО от стандартной библиотеки и от самого себя.

НЕ ИМЕЕТ права импортировать:
- application/
- infrastructure/
- bot/
- admin/
- любые внешние библиотеки кроме pydantic (для value objects) и тайпинговых утилит

Это правило проверяется в CI через import-linter.
"""
